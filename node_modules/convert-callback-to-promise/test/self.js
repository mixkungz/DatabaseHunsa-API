const convert = require('../index');

const timeout = function(time, cb){
  setTimeout(function(){
    cb();
  }, time);
}

const timeoutPromise = convert(timeout);

timeoutPromise(1000).then(() => console.log('hello world!'));